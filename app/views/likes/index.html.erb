
<h3>Likes</h3>
<div class="card">
    <div class="card-body">
            <div class="row">
            <% @tweet.likes.each do |l,i| %>
                    <div class="col-4 p-3">
                        <div class="card" style="width: 18rem;">
                            <img class="card-img-top" src=<%= url_for(l.user.avatar) if l.user.avatar.attached? %>" alt="Card image cap">
                            <div class="card-body">
                                <h5 class="card-title"><%= l.user.user_name %></h5>
                            </div>
                        </div>
                    </div>
            <% end %>
            </div>
        </div>
    </div>
    <%= link_to "Atrás", root_path , class: 'btn btn-secondary' %>
</div>
